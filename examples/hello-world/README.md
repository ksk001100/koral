# Hello World Example

This is a minimal example demonstrating the basic usage of the `koral` framework.
It shows how to define a simple application.

## Usage

```bash
# Run the application
cargo run -p hello-world
```
